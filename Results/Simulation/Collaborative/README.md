# Collaborative Simulation Results

The completed collaborative known-target visit dataset is in
[`Published/`](Published/README.md). Supporting source records remain under
`../Sensitivity/` and `../TopKLowKSupplement/` because those directories retain
the original campaign provenance.
